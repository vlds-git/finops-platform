{{- if .Values.recommendation.enabled }}
{{- include "finops.deployment" (dict "Root" . "Name" "recommendation" "Component" "recommendation" "ValuesKey" "recommendation" "Port" 8003 "Env" (list
  (dict "name" "CLICKHOUSE_ADDR" "value" (printf "%s-clickhouse:9000" (include "finops.fullname" .)))
  (dict "name" "USD_TO_BRL_RATE" "value" "5.15")
)) }}
{{- end }}
